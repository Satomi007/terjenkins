pipeline {
    agent any
    stages {
        stage('Init') {
            steps {
                echo 'Init'
                sh 'terraform init'
                
            }
        }
       
    }
}
